// does clicking cart icon open the cart flyout
// does clicking the logo take us back to home

describe.skip("This suite will be skipped", () => {
  it("should not run", () => {
    expect(true).toBe(false);
  });
});
