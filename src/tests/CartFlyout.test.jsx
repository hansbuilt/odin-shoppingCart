// does the math of the line items match the order total
// does the quantity increment button increase the qty and the ext price
// does the quantity decrement button decrease the qty and the ext price
// does qty from 1 to 0 remove the item from the cart
// does the cart close when clicking the overlay, keep shopping, and close cart

describe.skip("This suite will be skipped", () => {
  it("should not run", () => {
    expect(true).toBe(false);
  });
});
